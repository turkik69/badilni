{
  "name": "badilni-push-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node tests/multi-user.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.38.0"
  }
}
